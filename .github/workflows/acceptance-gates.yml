name: Acceptance Gates

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  evidence-gates:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify required evidence artifacts exist
        run: ci/check-evidence.sh ci/evidence-manifest.txt
